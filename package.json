{
  "name": "pdf-service",
  "version": "1.0.0",
  "description": "Stateless REST microservice: HTML template + data → PDF",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@asteasolutions/zod-to-openapi": "^7.0.0",
    "express": "^5.0.0",
    "helmet": "^8.0.0",
    "http-errors": "^2.0.0",
    "pino": "^9.0.0",
    "pino-http": "^10.0.0",
    "puppeteer": "^23.0.0",
    "swagger-ui-express": "^5.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0",
    "nodemon": "^3.0.0",
    "supertest": "^7.0.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
